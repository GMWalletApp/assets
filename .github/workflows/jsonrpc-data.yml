name: Update JSON-RPC Data

on:
  workflow_dispatch:
    inputs:
      sync_target:
        description: "JSON cache target to generate"
        required: false
        default: all
        type: choice
        options:
          - all
          - tokenlist
      market_limit:
        description: "CoinGecko market rows to fetch for market.json and tokenlist enrichment"
        required: false
        default: "1000"
  push:
    branches:
      - main
      - master
    paths-ignore:
      - "extensions/jsonrpc/data/**"

permissions:
  contents: write

concurrency:
  group: jsonrpc-data-${{ github.ref }}
  cancel-in-progress: true

jobs:
  update-jsonrpc-data:
    runs-on: ubuntu-latest
    if: github.actor != 'github-actions[bot]'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: extensions/jsonrpc/go.mod
          cache-dependency-path: extensions/jsonrpc/go.sum

      - name: Require CoinGecko API key
        env:
          COINGECKO_API_KEY: ${{ secrets.COINGECKO_API_KEY }}
        run: |
          if [ -z "$COINGECKO_API_KEY" ]; then
            echo "::error::COINGECKO_API_KEY repository secret is required to generate JSON-RPC data"
            exit 1
          fi

      - name: Test JSON-RPC sidecar
        working-directory: extensions/jsonrpc
        run: make test

      - name: Generate JSON-RPC data
        working-directory: extensions/jsonrpc
        env:
          COINGECKO_API_KEY: ${{ secrets.COINGECKO_API_KEY }}
          COINGECKO_API_BASE_URL: ${{ vars.COINGECKO_API_BASE_URL }}
          COINGECKO_API_KEY_HEADER: ${{ vars.COINGECKO_API_KEY_HEADER }}
          DEFILLAMA_STABLECOIN_BASE_URL: ${{ vars.DEFILLAMA_STABLECOIN_BASE_URL }}
          SYNC_TARGET_INPUT: ${{ github.event.inputs.sync_target || 'all' }}
          MARKET_LIMIT: ${{ github.event.inputs.market_limit || '1000' }}
        run: |
          sync_args="--sync-target ${SYNC_TARGET_INPUT} --market-limit ${MARKET_LIMIT}"
          make sync-once SYNC_ARGS="$sync_args"

      - name: Verify generated files
        env:
          SYNC_TARGET_INPUT: ${{ github.event.inputs.sync_target || 'all' }}
        run: |
          sync_target="${SYNC_TARGET_INPUT}"
          case "$sync_target" in
            all)
              files="market.json tokenlist.json tokenlist-report.json"
              ;;
            tokenlist)
              files="tokenlist.json tokenlist-report.json"
              ;;
            *)
              echo "::error::unsupported sync_target: $sync_target"
              exit 1
              ;;
          esac
          for file in $files; do
            test -s "extensions/jsonrpc/data/$file"
            python3 -m json.tool "extensions/jsonrpc/data/$file" >/dev/null
          done

      - name: Commit generated JSON
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add extensions/jsonrpc/data/market.json
          git add extensions/jsonrpc/data/tokenlist.json
          git add extensions/jsonrpc/data/tokenlist-report.json
          if git diff --cached --quiet; then
            echo "No JSON data changes to commit."
            exit 0
          fi
          git commit -m "chore: update jsonrpc data"
          git push
